{
  "name": "fitbit-sync",
  "version": "0.10.0",
  "private": true,
  "workspaces": [
    "backend",
    "shared"
  ],
  "scripts": {
    "start": "npm run start --workspace=backend",
    "dev": "npm run dev --workspace=backend"
  }
}
